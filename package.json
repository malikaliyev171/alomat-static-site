{
  "name": "alomat-static-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static bilingual .alomat editorial signal site.",
  "scripts": {
    "build": "node build.mjs",
    "preview": "node preview-server.cjs",
    "check": "npm run build && node scripts/check-site.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
